# Example agent env file (do not commit a real `.env`).

# Production (Render backend):
REMOTE_SERVER_URL=https://calling-app-backend-1.onrender.com

# Local dev (uncomment one approach):
# REMOTE_USE_LOCALHOST=1
# REMOTE_SERVER_URL=http://localhost:5000

# Optional. Leave empty to auto-generate a stable host id for this device.
REMOTE_HOST_ID=
REMOTE_FPS=6
# Optional verbose input-event logs (for troubleshooting only).
# REMOTE_DEBUG=1

# Must match backend REMOTE_CONTROL_TOKEN if backend auth is enabled.
# REMOTE_CONTROL_TOKEN=change-me
